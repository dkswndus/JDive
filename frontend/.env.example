# 이 파일을 frontend/.env.local 로 복사해서 쓴다. .env.local 은 커밋하지 않는다.

# 동일 출처 프록시(/api/v1/*)가 전달할 백엔드 주소. next dev / next build 시점에 읽는다.
BACKEND_INTERNAL_URL=http://localhost:8000

# 프론트엔드 오류 수집(Sentry). 비워 두면 비활성이다. DSN 은 공개 값이다.
NEXT_PUBLIC_SENTRY_DSN=
